# 🧮 Counter Application (React useState Project)

A simple and interactive Counter Application built using **React**.  
This project demonstrates how to use the **useState** hook to handle and update component state dynamically.

---

## 📌 Project Objective

The main goal of this project is to understand how React state works and how UI updates automatically when the state changes.  
This project covers:

- Creating functional components  
- Using the `useState` hook  
- Updating state on button clicks  
- Conditional rendering  
- Basic UI styling  
- Preventing invalid counter values  

---

## 🚀 Features

✔ Increment the counter  
✔ Decrement the counter  
✔ Reset the counter to zero  
✔ Prevent counter from going below zero  
✔ Display warning message when count reaches minimum  
✔ Modern and responsive UI  
✔ Smooth animations  
✔ Clean component structure  

---

## 🧩 Technologies Used

- **React.js**
- **JavaScript (ES6+)**
- **CSS3**
- **Vite / Create React App** (any setup works)

---

## 📁 Project Structure

